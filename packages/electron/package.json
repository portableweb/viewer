{
  "name": "@portableweb/electron",
  "version": "0.1.0",
  "private": true,
  "description": "Electron desktop viewer — reference implementation. Windows/macOS/Linux from one codebase.",
  "main": "dist/src/main.js",
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "start": "npm run build && electron .",
    "test": "npm run build && node --test ./dist/test/*.test.js",
    "dist": "npm run build && electron-builder"
  },
  "license": "MIT",
  "author": "Omprakash Selvaraj",
  "dependencies": {
    "@portableweb/core": "^0.1.0",
    "@portableweb/shims": "^0.1.0",
    "@portableweb/store": "^0.1.0"
  },
  "devDependencies": {
    "electron": "^33.0.0",
    "electron-builder": "^25.0.0",
    "typescript": "^5.5.0",
    "@types/node": "^22.0.0"
  }
}
