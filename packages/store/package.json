{
  "name": "@portableweb/store",
  "version": "0.1.0",
  "private": true,
  "description": "Shared store schema, serializer, and Node fs persistence transport for isolated per-bundle storage",
  "main": "dist/src/index.js",
  "types": "dist/src/index.d.ts",
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "test": "npm run build && node --test ./dist/test/*.test.js"
  },
  "license": "MIT",
  "devDependencies": {
    "typescript": "^5.5.0",
    "@types/node": "^22.0.0"
  }
}
