{
  "name": "@portableweb/shims",
  "version": "0.1.0",
  "private": true,
  "description": "Document-start localStorage shim, parameterized by a Transport bridge the adapter provides",
  "main": "dist/src/index.js",
  "types": "dist/src/index.d.ts",
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "test": "npm run build && node --test ./dist/test/*.test.js"
  },
  "license": "MIT",
  "devDependencies": {
    "typescript": "^5.5.0",
    "@types/node": "^22.0.0"
  }
}
