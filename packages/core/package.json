{
  "name": "@portableweb/core",
  "version": "0.1.0",
  "description": "Pure TS container/manifest/resolution logic for PortableWeb bundles — no platform APIs",
  "main": "dist/src/index.js",
  "types": "dist/src/index.d.ts",
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "test": "npm run build && node --test ./dist/test/*.test.js"
  },
  "license": "MIT",
  "devDependencies": {
    "typescript": "^5.5.0",
    "@types/node": "^22.0.0"
  }
}
